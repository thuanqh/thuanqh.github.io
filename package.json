{
  "name": "my-blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nextein",
    "export": "nextein build && nextein export",
    "deploy": "rm -rf node_modules/.cache && nextein build && nextein export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy personal blog to github pages\" && git subtree push --prefix out origin v0.0.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "next": "^6.1.1",
    "nextein": "^1.3.1",
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  }
}
